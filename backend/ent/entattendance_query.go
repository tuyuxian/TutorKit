// Code generated by ent, DO NOT EDIT.

package ent

import (
	"backend/ent/entattendance"
	"backend/ent/predicate"
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// EntAttendanceQuery is the builder for querying EntAttendance entities.
type EntAttendanceQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.EntAttendance
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the EntAttendanceQuery builder.
func (eaq *EntAttendanceQuery) Where(ps ...predicate.EntAttendance) *EntAttendanceQuery {
	eaq.predicates = append(eaq.predicates, ps...)
	return eaq
}

// Limit adds a limit step to the query.
func (eaq *EntAttendanceQuery) Limit(limit int) *EntAttendanceQuery {
	eaq.limit = &limit
	return eaq
}

// Offset adds an offset step to the query.
func (eaq *EntAttendanceQuery) Offset(offset int) *EntAttendanceQuery {
	eaq.offset = &offset
	return eaq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (eaq *EntAttendanceQuery) Unique(unique bool) *EntAttendanceQuery {
	eaq.unique = &unique
	return eaq
}

// Order adds an order step to the query.
func (eaq *EntAttendanceQuery) Order(o ...OrderFunc) *EntAttendanceQuery {
	eaq.order = append(eaq.order, o...)
	return eaq
}

// First returns the first EntAttendance entity from the query.
// Returns a *NotFoundError when no EntAttendance was found.
func (eaq *EntAttendanceQuery) First(ctx context.Context) (*EntAttendance, error) {
	nodes, err := eaq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{entattendance.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (eaq *EntAttendanceQuery) FirstX(ctx context.Context) *EntAttendance {
	node, err := eaq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first EntAttendance ID from the query.
// Returns a *NotFoundError when no EntAttendance ID was found.
func (eaq *EntAttendanceQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = eaq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{entattendance.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (eaq *EntAttendanceQuery) FirstIDX(ctx context.Context) int {
	id, err := eaq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single EntAttendance entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one EntAttendance entity is found.
// Returns a *NotFoundError when no EntAttendance entities are found.
func (eaq *EntAttendanceQuery) Only(ctx context.Context) (*EntAttendance, error) {
	nodes, err := eaq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{entattendance.Label}
	default:
		return nil, &NotSingularError{entattendance.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (eaq *EntAttendanceQuery) OnlyX(ctx context.Context) *EntAttendance {
	node, err := eaq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only EntAttendance ID in the query.
// Returns a *NotSingularError when more than one EntAttendance ID is found.
// Returns a *NotFoundError when no entities are found.
func (eaq *EntAttendanceQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = eaq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{entattendance.Label}
	default:
		err = &NotSingularError{entattendance.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (eaq *EntAttendanceQuery) OnlyIDX(ctx context.Context) int {
	id, err := eaq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of EntAttendances.
func (eaq *EntAttendanceQuery) All(ctx context.Context) ([]*EntAttendance, error) {
	if err := eaq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return eaq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (eaq *EntAttendanceQuery) AllX(ctx context.Context) []*EntAttendance {
	nodes, err := eaq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of EntAttendance IDs.
func (eaq *EntAttendanceQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := eaq.Select(entattendance.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (eaq *EntAttendanceQuery) IDsX(ctx context.Context) []int {
	ids, err := eaq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (eaq *EntAttendanceQuery) Count(ctx context.Context) (int, error) {
	if err := eaq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return eaq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (eaq *EntAttendanceQuery) CountX(ctx context.Context) int {
	count, err := eaq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (eaq *EntAttendanceQuery) Exist(ctx context.Context) (bool, error) {
	if err := eaq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return eaq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (eaq *EntAttendanceQuery) ExistX(ctx context.Context) bool {
	exist, err := eaq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the EntAttendanceQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (eaq *EntAttendanceQuery) Clone() *EntAttendanceQuery {
	if eaq == nil {
		return nil
	}
	return &EntAttendanceQuery{
		config:     eaq.config,
		limit:      eaq.limit,
		offset:     eaq.offset,
		order:      append([]OrderFunc{}, eaq.order...),
		predicates: append([]predicate.EntAttendance{}, eaq.predicates...),
		// clone intermediate query.
		sql:    eaq.sql.Clone(),
		path:   eaq.path,
		unique: eaq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
func (eaq *EntAttendanceQuery) GroupBy(field string, fields ...string) *EntAttendanceGroupBy {
	grbuild := &EntAttendanceGroupBy{config: eaq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := eaq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return eaq.sqlQuery(ctx), nil
	}
	grbuild.label = entattendance.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
func (eaq *EntAttendanceQuery) Select(fields ...string) *EntAttendanceSelect {
	eaq.fields = append(eaq.fields, fields...)
	selbuild := &EntAttendanceSelect{EntAttendanceQuery: eaq}
	selbuild.label = entattendance.Label
	selbuild.flds, selbuild.scan = &eaq.fields, selbuild.Scan
	return selbuild
}

func (eaq *EntAttendanceQuery) prepareQuery(ctx context.Context) error {
	for _, f := range eaq.fields {
		if !entattendance.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if eaq.path != nil {
		prev, err := eaq.path(ctx)
		if err != nil {
			return err
		}
		eaq.sql = prev
	}
	return nil
}

func (eaq *EntAttendanceQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*EntAttendance, error) {
	var (
		nodes = []*EntAttendance{}
		_spec = eaq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]interface{}, error) {
		return (*EntAttendance).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []interface{}) error {
		node := &EntAttendance{config: eaq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, eaq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (eaq *EntAttendanceQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := eaq.querySpec()
	_spec.Node.Columns = eaq.fields
	if len(eaq.fields) > 0 {
		_spec.Unique = eaq.unique != nil && *eaq.unique
	}
	return sqlgraph.CountNodes(ctx, eaq.driver, _spec)
}

func (eaq *EntAttendanceQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := eaq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %w", err)
	}
	return n > 0, nil
}

func (eaq *EntAttendanceQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   entattendance.Table,
			Columns: entattendance.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: entattendance.FieldID,
			},
		},
		From:   eaq.sql,
		Unique: true,
	}
	if unique := eaq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := eaq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, entattendance.FieldID)
		for i := range fields {
			if fields[i] != entattendance.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := eaq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := eaq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := eaq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := eaq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (eaq *EntAttendanceQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(eaq.driver.Dialect())
	t1 := builder.Table(entattendance.Table)
	columns := eaq.fields
	if len(columns) == 0 {
		columns = entattendance.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if eaq.sql != nil {
		selector = eaq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if eaq.unique != nil && *eaq.unique {
		selector.Distinct()
	}
	for _, p := range eaq.predicates {
		p(selector)
	}
	for _, p := range eaq.order {
		p(selector)
	}
	if offset := eaq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := eaq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// EntAttendanceGroupBy is the group-by builder for EntAttendance entities.
type EntAttendanceGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (eagb *EntAttendanceGroupBy) Aggregate(fns ...AggregateFunc) *EntAttendanceGroupBy {
	eagb.fns = append(eagb.fns, fns...)
	return eagb
}

// Scan applies the group-by query and scans the result into the given value.
func (eagb *EntAttendanceGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := eagb.path(ctx)
	if err != nil {
		return err
	}
	eagb.sql = query
	return eagb.sqlScan(ctx, v)
}

func (eagb *EntAttendanceGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range eagb.fields {
		if !entattendance.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := eagb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := eagb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (eagb *EntAttendanceGroupBy) sqlQuery() *sql.Selector {
	selector := eagb.sql.Select()
	aggregation := make([]string, 0, len(eagb.fns))
	for _, fn := range eagb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	// If no columns were selected in a custom aggregation function, the default
	// selection is the fields used for "group-by", and the aggregation functions.
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(eagb.fields)+len(eagb.fns))
		for _, f := range eagb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(eagb.fields...)...)
}

// EntAttendanceSelect is the builder for selecting fields of EntAttendance entities.
type EntAttendanceSelect struct {
	*EntAttendanceQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Scan applies the selector query and scans the result into the given value.
func (eas *EntAttendanceSelect) Scan(ctx context.Context, v interface{}) error {
	if err := eas.prepareQuery(ctx); err != nil {
		return err
	}
	eas.sql = eas.EntAttendanceQuery.sqlQuery(ctx)
	return eas.sqlScan(ctx, v)
}

func (eas *EntAttendanceSelect) sqlScan(ctx context.Context, v interface{}) error {
	rows := &sql.Rows{}
	query, args := eas.sql.Query()
	if err := eas.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
